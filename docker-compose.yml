version: '3'

services:
  blynk:
    image: mpherg/blynk-server:latest
    ports:
      - "8080:8080"
      - "8441:8441"
      - "9443:9443"
    #volumes:
    #  - /var/run/docker.sock:/tmp/docker.sock:ro
    restart: always
